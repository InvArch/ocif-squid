type Staker @entity {
  id: ID!
  latestClaimBlock: Int!
  account: String!
  totalRewards: String!
  totalUnclaimed: String!
}

type Core @entity {
  id: ID!
  latestClaimBlock: Int!
  coreId: Int!
  totalRewards: String!
  totalUnclaimed: String!
}